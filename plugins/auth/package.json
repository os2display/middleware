{
  "name": "Auth",
  "description": "Provides authentication with API-key to the API",
  "version": "0.0.1",
  "main": "auth.js",
  "private": true,
  "dependencies": {
    "express-jwt": "^3.0.1",
    "jsonwebtoken": "^5.0.1",
    "request-json": "^0.5.3"
  },
  "plugin": {
    "provides": [
      "auth"
    ],
    "consumes": [
      "app",
      "logger",
      "apikeys"
    ]
  }
}
